[package]
name = "pii-leak-audit"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
valayam-plugin-sdk = { path = "../../crates/valayam-plugin-sdk" }
regex = "1.10"
serde_json = "1.0"
