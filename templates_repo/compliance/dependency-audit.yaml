id: compliance-dependency-audit
info:
  name: "Dependency Vulnerability Audit"
  severity: "Critical"
  description: "Cross-references project dependencies against known CVE databases using local vuln DB."
  compliance:
    cwe: "CWE-1104"
dependency_audit:
  - target: "{{Hostname}}"
    target_repo: "."