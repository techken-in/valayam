//! Scan state persistence — delegated to `valayam-state` crate.
//!
//! Re-exports `StateDB` and `ScanSnapshot` for backward compatibility.
//! All implementation lives in `crates/valayam-state`.

pub use valayam_state::*;
